style: apply prettier across ts/tsx/md; align eslint with prettier

- Format the full CI surface (**/*.{ts,tsx,md}) so `prettier --check`
  passes repo-wide (467 files drifted)
- Disable unicorn/no-nested-ternary in the desktop ESLint config: it
  fights Prettier's ternary chain formatting (parens stripped on every
  Prettier pass re-triggering the lint). Prettier is the formatter of
  record; both gates now pass simultaneously and stably
- eslint --fix applied for mechanical violations surfaced by the
  normalization (nested ternaries, import ordering)
- Mobile tree converted to LF as part of the sweep; its remaining lint
  output is warning-level only (exit 0) and clears fully after
  `git add --renormalize .` + fresh checkout on LF platforms

No behavioral changes: desktop jest 348/348, tsc clean, ESLint clean,
automation-dsl 36/36, mobile type-check clean and vault suite 9/9 all
re-verified after formatting.
